---
export interface Props{
    className?:string
    dataTranslate?:string
}

const {className, dataTranslate} = Astro.props
---

<p class={`md:text-lg text-heading-3 ${className}`} data-translate={dataTranslate}>
    <slot/>
</p>